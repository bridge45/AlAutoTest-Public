name: AlRun

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  run-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 构建 Docker 镜像
      run: |
        echo "🐳 构建 Docker 镜像..."
        docker build -t alautotest:latest -f docker/Dockerfile .
        echo "✅ Docker 镜像构建完成"
          
    - name: 在 Docker 容器中执行 run.sh
      run: |
        echo "🚀 在 Docker 容器中执行 run.sh..."
        docker run --rm \
          -v "$(pwd):/workspace" \
          -w /workspace \
          alautotest:latest \
          bash -c "chmod +x run.sh && ./run.sh && echo '=== 执行结果如下 ===' && cat <(echo '--- stdout ---') <(./run.sh)"
        echo "✅ run.sh 执行完成"